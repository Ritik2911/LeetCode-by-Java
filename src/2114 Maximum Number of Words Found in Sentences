class Solution {
    public int mostWordsFound(String[] sentences) {
    
        int ans = 0;
        
        for(int i = 0; i < sentences.length; i++){
            String[] sentence = sentences[i].split(" ");
            ans = Math.max(ans, sentence.length);
        }
        
        return ans;
    }
}
